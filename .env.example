# Build-time configuration for the frontend (optional — most users don't need it).
# If unset, the app uses the WeatherAPI key compiled into src/lib/config.js.
# To switch keys without editing source, copy this to .env and set:
REACT_APP_WEATHER_API_KEY=your_weatherapi_key