# Frontend build-time configuration (optional in dev — the CRA dev server
# proxies /api to http://localhost:5000 automatically).
#
# Production on a separate host (e.g. GitHub Pages) that is NOT the backend:
# REACT_APP_API_URL=https://your-backend-host.example
REACT_APP_API_URL=